# as_coordinates errors

    Code
      as_coordinates(v)
    Condition
      Error in `as_coordinates()`:
      ! `tidyterra::as_coordinates()` needs a <SpatRaster> object, not a <matrix/array> object

