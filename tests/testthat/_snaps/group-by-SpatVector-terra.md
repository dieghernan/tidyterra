# Gives meaningful messages

    Code
      gr_v2 <- mutate(gr_v2, a = 1)
    Message
      ! `tidyterra::group_vars.SpatVector()` missing on data.  Have you mixed terra and tidyterra syntax?
      i Ungrouping data

