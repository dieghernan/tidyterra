# reframe() validates .dissolve

    Code
      reframe(v, value = 1, .dissolve = "yes")
    Condition
      Error in `reframe()`:
      ! `.dissolve` must be `TRUE` or `FALSE`, not the string "yes".

